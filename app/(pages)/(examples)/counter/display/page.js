import { DisplayValue } from "./value";

export const metadata = {
  title: "Ejemplo - Display Counter",
  description: "Display Counter",
};

export default function DisplayPage() {
  return <DisplayValue />;
}
